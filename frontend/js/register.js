/*
const config = require("./config");

const router = config.express.Router();

router.get("/register", function (request, response) {
  // Render login template
  response.sendFile("../html/register.html");
});
*/